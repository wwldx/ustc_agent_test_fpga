# Tests

No project behavior tests exist yet.

Current baseline check:

```bash
bash scripts/check_repo_safety.sh
```

When deterministic skills are added, add focused tests here and record the command in `AGENTS.md`.
