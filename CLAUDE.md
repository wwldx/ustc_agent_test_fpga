@AGENTS.md

## Claude Code Notes

- Follow the shared project rules in `AGENTS.md`.
- Keep Claude-specific behavior here only.
- When adapting a Verilog-agent sub-agent into Claude Code, record the role, allowed tools, verification loop, and failure mode in `prompts/` or the relevant experiment directory.
- Do not let Claude Code write credentials, local auth files, raw lab data, private IP, or unpublished graduation materials into this public repository.
